import 'package:flutter/material.dart';

/// Palet warna resmi dan modern PTSP Kemenag Barito Utara
class AppColors {
  AppColors._();

  // Brand Kemenag (Deep Emerald Green)
  static const Color primary = Color(0xFF064E3B);
  static const Color primaryMedium = Color(0xFF059669);
  static const Color primaryLight = Color(0xFF10B981);
  static const Color primarySoft = Color(0xFFD1FAE5);

  // Secondary & Accents (Gold / Amber Kemenag)
  static const Color secondary = Color(0xFFD97706);
  static const Color secondaryLight = Color(0xFFFDE68A);

  // Background & Surfaces
  static const Color background = Color(0xFFF8FAFC);
  static const Color surface = Colors.white;
  static const Color cardBorder = Color(0xFFE2E8F0);

  // Text Colors
  static const Color textPrimary = Color(0xFF0F172A);
  static const Color textSecondary = Color(0xFF475569);
  static const Color textMuted = Color(0xFF94A3B8);

  // Status Badges
  static const Color statusSuccess = Color(0xFF16A34A);
  static const Color statusSuccessBg = Color(0xFFDCFCE7);
  static const Color statusPending = Color(0xFFCA8A04);
  static const Color statusPendingBg = Color(0xFFFEF9C3);
  static const Color statusRejected = Color(0xFFDC2626);
  static const Color statusRejectedBg = Color(0xFFFEE2E2);
  static const Color statusProcess = Color(0xFF2563EB);
  static const Color statusProcessBg = Color(0xFFDBEAFE);
}
